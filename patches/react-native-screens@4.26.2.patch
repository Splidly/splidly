diff --git a/ios/RNSBarButtonItem.mm b/ios/RNSBarButtonItem.mm
index 0eb1f09dee82edd99e3e614938233db5cdeaebfe..57fb3e27191798ddacd906376ba33c9100fee8c1 100644
--- a/ios/RNSBarButtonItem.mm
+++ b/ios/RNSBarButtonItem.mm
@@ -315,6 +315,5 @@ - (void)setTitleStyleFromConfig:(NSDictionary *)titleStyle
   [self setTitleTextAttributes:attrs forState:UIControlStateNormal];
   [self setTitleTextAttributes:attrs forState:UIControlStateHighlighted];
   [self setTitleTextAttributes:attrs forState:UIControlStateDisabled];
-  [self setTitleTextAttributes:attrs forState:UIControlStateSelected];
   [self setTitleTextAttributes:attrs forState:UIControlStateFocused];
 }
diff --git a/ios/RNSScreenStackHeaderConfig.mm b/ios/RNSScreenStackHeaderConfig.mm
index 1c844846a5c66e31cfa530ca462774d2fb6bbea1..964243f9daa6ae739003744d93a793eda81d7ec2 100644
--- a/ios/RNSScreenStackHeaderConfig.mm
+++ b/ios/RNSScreenStackHeaderConfig.mm
@@ -273,6 +273,5 @@ + (void)setTitleAttibutes:(NSDictionary *)attrs forButton:(UIBarButtonItem *)but
   [button setTitleTextAttributes:attrs forState:UIControlStateNormal];
   [button setTitleTextAttributes:attrs forState:UIControlStateHighlighted];
   [button setTitleTextAttributes:attrs forState:UIControlStateDisabled];
-  [button setTitleTextAttributes:attrs forState:UIControlStateSelected];
   [button setTitleTextAttributes:attrs forState:UIControlStateFocused];
 }
@@ -518,11 +517,22 @@ + (void)updateViewController:(UIViewController *)vc
 #if !TARGET_OS_TV
   [config configureBackItem:prevItem withPrevVC:prevVC];
 
-  if (config.largeTitle) {
+  // Expo Router does not expose UIKit's inline-large display mode. An explicit
+  // large-title font size while collapsible large titles are disabled is used
+  // as the opt-in signal so the JS navigator never enters its large-header
+  // scroll-inset path.
+  BOOL inlineLargeTitle = !config.largeTitle && config.largeTitleFontSize != nil;
+  if (config.largeTitle || inlineLargeTitle) {
     navctr.navigationBar.prefersLargeTitles = YES;
   }
-  navitem.largeTitleDisplayMode =
-      config.largeTitle ? UINavigationItemLargeTitleDisplayModeAlways : UINavigationItemLargeTitleDisplayModeNever;
+  if (@available(iOS 17.0, *)) {
+    navitem.largeTitleDisplayMode = inlineLargeTitle ? UINavigationItemLargeTitleDisplayModeInline
+        : config.largeTitle ? UINavigationItemLargeTitleDisplayModeAlways
+                            : UINavigationItemLargeTitleDisplayModeNever;
+  } else {
+    navitem.largeTitleDisplayMode = config.largeTitle ? UINavigationItemLargeTitleDisplayModeAlways
+                                                      : UINavigationItemLargeTitleDisplayModeNever;
+  }
 #endif
 
   UINavigationBarAppearance *appearance = [self buildAppearance:vc withConfig:config];
